# ⚠️ INTENTIONALLY VULNERABLE — EDUCATION ONLY. VULN-27: secret rendered inline from plaintext values.
# Fix: source from Vault/ESO — see ../../../ecommerce/helm/shop/templates/externalsecret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: vulnerable-shop-credentials
type: Opaque
stringData:
  DB_PASSWORD: "{{ .Values.plaintextSecrets.dbPassword }}"
  API_KEY: "{{ .Values.plaintextSecrets.apiKey }}"
